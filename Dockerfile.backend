# Usa una imagen oficial de Node basada en Debian/Ubuntu
FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci --production || npm install --production

COPY server.js ./

EXPOSE 3000

CMD ["node", "server.js"]